<div >	
	<p> Lorem ipsum index </p>
	<div id='imgur_upload' style="width: 100%; height: 200px;">
		<form:form id="userForm" method="post" commandName="user" enctype="multipart/form-data" data-ajax="false">
		</form>
	</div>
	<div id='map_canvas' style="width: 100%; height: 400px;"></div>
</div>
<script>
		// initialise the google map
		var map = new google.maps.Map(document.getElementById('map_canvas'), {
		    center: new google.maps.LatLng(40.723844,-73.995667),
		    zoom: 17,
		    mapTypeId: google.maps.MapTypeId.SATELLITE,
		    mapTypeControl: false
		});
        map.setMapTypeId(google.maps.MapTypeId.ROADMAP);
		Reinvent(function(env) {
		  Reinvent.app = new env.app.Instance(map, {
		        logging    : true
		    });
		  Reinvent.app.run();
		  Reinvent.env = env;
		});
</script>